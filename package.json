{
  "name": "gyazo-api-sdk-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "build": "pnpm --recursive run build",
    "changeset": "changeset",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run test:coverage && pnpm run build",
    "check:native": "pnpm --filter gyazoctl run check:native && pnpm --filter gyazoctl run build:native && node scripts/native-smoke.mjs packages/gyazoctl/dist/native/gyazoctl",
    "check:packages": "publint packages/gyazo-api-sdk && publint packages/gyazoctl && attw --pack --profile esm-only packages/gyazo-api-sdk && attw --pack --profile esm-only packages/gyazoctl",
    "format": "biome check . --write",
    "lint": "biome check .",
    "release": "changeset publish",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "version-packages": "changeset version && node scripts/sync-cli-version.mjs"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "0.18.5",
    "@biomejs/biome": "2.5.7",
    "@changesets/cli": "2.31.1",
    "@types/node": "26.2.0",
    "@vitest/coverage-v8": "4.1.10",
    "publint": "0.3.23",
    "scriptc": "0.0.23",
    "tsdown": "0.22.14",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
